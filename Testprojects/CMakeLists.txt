cmake_minimum_required(VERSION 2.8)
execute_process(COMMAND cmake ClientServer/CMakeLists.txt RESULT_VARIABLE failure)
if(failure)
	message(FATAL_ERROR "Failed generating ClientServer project")
endif(failure)
execute_process(COMMAND cmake AliceBob/CMakeLists.txt RESULT_VARIABLE failure)
if(failure)
	message(FATAL_ERROR "Failed generating AliceBob project")
endif(failure)
