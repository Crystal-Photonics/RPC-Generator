#define RPC_SEND(BUFFER, SIZE) (send((BUFFER), (SIZE)))
#define RPC_SLEEP() (aquire_semaphor(send_semaphor) != semaphor_timeout)
#define RPC_WAKEUP() (release_semaphor(send_semaphor))

uint8_t f(uint16_t i, uint8_t singleArrayData_in[1][17], uint8_t singleArrayData_out[2][12], uint8_t singleArrayData_inout[3][43]);

RPC_RESULT f(uint8_t return_value_out[1], uint16_t i, uint8_t singleArrayData_in[1][17], uint8_t singleArrayData_out[2][12], uint8_t singleArrayData_inout[3][43]);
 
RPC_RESULT f(uint8_t return_value_out[1], uint16_t i, uint8_t singleArrayData_in[1][17], uint8_t singleArrayData_out[2][12], uint8_t singleArrayData_inout[3][43]){
	/***Serializing***/
	char *current = sendBuffer;
	*current++ = 2; /* save ID */

	/*Skipping array "return_value_out", because it is not an input parameter*/
	/* writing basic type uint16_t i of size 2 */
	memcpy(current, &i, 2);
	current += 2;
	/* writing array singleArrayData_in with 1 elements */
	{
		int RPC_COUNTER_VAR1;
		for (RPC_COUNTER_VAR1 = 0; RPC_COUNTER_VAR1 < 1; RPC_COUNTER_VAR1++){

			/* writing array singleArrayData_in[RPC_COUNTER_VAR1] with 17 elements */
			{
				int RPC_COUNTER_VAR3;
				for (RPC_COUNTER_VAR3 = 0; RPC_COUNTER_VAR3 < 17; RPC_COUNTER_VAR3++){

					/* writing basic type uint8_t singleArrayData_in[RPC_COUNTER_VAR1][RPC_COUNTER_VAR3] of size 1 */
					memcpy(current, &singleArrayData_in[RPC_COUNTER_VAR1][RPC_COUNTER_VAR3], 1);
					current += 1;
				}
			}
		}
	}
	/*Skipping array "singleArrayData_out", because it is not an input parameter*/
	/* writing array singleArrayData_inout with 3 elements */
	{
		int RPC_COUNTER_VAR1;
		for (RPC_COUNTER_VAR1 = 0; RPC_COUNTER_VAR1 < 3; RPC_COUNTER_VAR1++){

			/* writing array singleArrayData_inout[RPC_COUNTER_VAR1] with 43 elements */
			{
				int RPC_COUNTER_VAR3;
				for (RPC_COUNTER_VAR3 = 0; RPC_COUNTER_VAR3 < 43; RPC_COUNTER_VAR3++){

					/* writing basic type uint8_t singleArrayData_inout[RPC_COUNTER_VAR1][RPC_COUNTER_VAR3] of size 1 */
					memcpy(current, &singleArrayData_inout[RPC_COUNTER_VAR1][RPC_COUNTER_VAR3], 1);
					current += 1;
				}
			}
		}
	}
	send(start, current - sendBuffer);
	if (!(RPC_SLEEP()))
		return RPC_FAILURE;

	/***Deserializing***/
	
	/* reading array return_value_out with 1 elements */
	{
		int RPC_COUNTER_VAR1;
		for (RPC_COUNTER_VAR1 = 0; RPC_COUNTER_VAR1 < 1; RPC_COUNTER_VAR1++){

			/* reading basic type uint8_t return_value_out[RPC_COUNTER_VAR1] of size 1 */
			memcpy(&return_value_out[RPC_COUNTER_VAR1], current, 1);
			current += 1;
		}
	}
	/*Skipping array "singleArrayData_in, because it is not an output parameter*/
	/* reading array singleArrayData_out with 2 elements */
	{
		int RPC_COUNTER_VAR1;
		for (RPC_COUNTER_VAR1 = 0; RPC_COUNTER_VAR1 < 2; RPC_COUNTER_VAR1++){

			/* reading array singleArrayData_out[RPC_COUNTER_VAR1] with 12 elements */
			{
				int RPC_COUNTER_VAR3;
				for (RPC_COUNTER_VAR3 = 0; RPC_COUNTER_VAR3 < 12; RPC_COUNTER_VAR3++){

					/* reading basic type uint8_t singleArrayData_out[RPC_COUNTER_VAR1][RPC_COUNTER_VAR3] of size 1 */
					memcpy(&singleArrayData_out[RPC_COUNTER_VAR1][RPC_COUNTER_VAR3], current, 1);
					current += 1;
				}
			}
		}
	}
	/* reading array singleArrayData_inout with 3 elements */
	{
		int RPC_COUNTER_VAR1;
		for (RPC_COUNTER_VAR1 = 0; RPC_COUNTER_VAR1 < 3; RPC_COUNTER_VAR1++){

			/* reading array singleArrayData_inout[RPC_COUNTER_VAR1] with 43 elements */
			{
				int RPC_COUNTER_VAR3;
				for (RPC_COUNTER_VAR3 = 0; RPC_COUNTER_VAR3 < 43; RPC_COUNTER_VAR3++){

					/* reading basic type uint8_t singleArrayData_inout[RPC_COUNTER_VAR1][RPC_COUNTER_VAR3] of size 1 */
					memcpy(&singleArrayData_inout[RPC_COUNTER_VAR1][RPC_COUNTER_VAR3], current, 1);
					current += 1;
				}
			}
		}
	}
	return RPC_SUCCESS;
}